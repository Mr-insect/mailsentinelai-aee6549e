# Frontend public config only — NEVER put secrets in VITE_* variables.
# The browser calls the FastAPI backend; the backend holds all API keys.
# Leave empty to use the built-in offline demo engine (no backend required).
VITE_MAILSENTINEL_API_BASE=
